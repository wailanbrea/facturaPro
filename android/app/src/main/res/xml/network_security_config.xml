<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production default: HTTPS only, no cleartext allowed. -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Cleartext is permitted only against local development hosts
         (Android emulator loopback, localhost). Shipped builds talk to an
         HTTPS endpoint, so these exceptions are harmless in release. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
